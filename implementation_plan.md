# IATF 16949 Smart Platform 发布准备与迭代实施计划

该计划针对您提出的 6 项待办事项，提供了详细的建议、技术方案以及执行步骤。在您审核并确认此计划后，我将依次开始执行。

## > [!WARNING] 
## User Review Required (需要您确认的关键决策)

1. **API 密钥安全风险**：目前应用在前端直接调用 Gemini API（`process.env.GEMINI_API_KEY`）。在生产环境中发布时，任何用户都可以通过浏览器开发者工具获取该密钥并盗用。**建议**：如果您只在内部小范围使用，可以直接发布；如果是公开发布给大量陌生人，必须建立一个简单的后端代理（如 Vercel Serverless Functions）。请问您的发布受众是谁？希望如何处理？
2. **二维码素材**：增加捐赠功能需要您的微信/支付宝收款二维码图片。我将先使用占位图开发组件，后续需要您将真实的二维码图片放到 `public/` 目录下。
3. **PPT 风格偏好**：对于宣传 PPT，您是否有特定的颜色偏好（比如目前的红+黑+石灰经典配色），以及侧重点（偏向向老板汇报，还是面向普通用户的产品推介）？

---

## 阶段一：新功能开发 (待办项 1 & 2)

### 1. 增加捐赠功能 (二维码收款)
- **方案**：在应用底部的 Footer 以及“关于(Version)”页面，增加一个醒目的“☕ 赞助支持”按钮。点击后弹出一个精美的 Modal（模态框），采用玻璃拟物化设计，展示您的微信和支付宝收款二维码，并附上感谢语。
- **文件变更**：
  - `[NEW]` `src/components/DonationModal.tsx`
  - `[MODIFY]` `src/App.tsx` (在页脚增加入口)

### 2. 增加版权归属说明
- **方案**：
  - 在页脚明确声明平台的开发版权（如：© 2026 您的名称/团队）。
  - 增加“免责与版权声明”弹窗（Disclaimer）。明确说明：“本平台旨在辅助学习与应用，IATF 16949 官方标准文本及版权归属 AIAG/IATF 所有。平台生成的 AI 建议仅供参考，不作为正式审核依据。”
- **文件变更**：
  - `[NEW]` `src/components/DisclaimerModal.tsx`
  - `[MODIFY]` `src/App.tsx` (在页脚增加“免责声明”链接)

---

## 阶段二：项目审查与优化 (待办项 4)

### 1. 清理与重构
- **操作**：扫描 `package.json` 移除未使用的依赖；删除开发过程中可能残留的测试文件（如 `scratch` 脚本）。
- **优化**：检查是否存在控制台报错或警告（如 React 的 `key` 缺失）。

### 2. 性能与体验优化
- **操作**：
  - 为 Vite 配置打包优化（分块打包 `vendor` 库，如 `lucide-react` 和 `framer-motion`），提升首屏加载速度。
  - 完善 PWA（渐进式 Web 应用）相关的 `index.html` `meta` 标签，确保在移动端显示完美。

### 3. 版本迭代
- **操作**：更新 `src/data/versionData.ts`，将版本号从 Beta 推进到 `v1.0.0 (Release Candidate)`，并写入本次加入捐赠、版权和性能优化的 Release Notes。

---

## 阶段三：部署与发布准备 (待办项 6)

### 1. 完善 SEO 与 Meta 标签
- **操作**：修改 `index.html`，添加专业的 `<title>`、`<meta description>`、`<meta keywords>` 以及 Open Graph 标签（用于微信、钉钉分享时显示漂亮的卡片）。

### 2. 部署方案建议 (Vercel)
- **推荐方案**：强烈推荐使用 **Vercel** 免费托管。它是目前对前端 Vite/React 项目最友好的平台。
- **流程**：将代码推送到 GitHub/Gitee -> 授权 Vercel 访问该仓库 -> 自动构建并分配免费 HTTPS 域名（支持绑定自定义域名）。我将为您编写一份极其简单的《一键部署指南》。

---

## 阶段四：生成宣传 PPT (待办项 5)

- **方案**：使用 Python 的 `pptx` 自动化生成一份专业的宣传幻灯片。
- **PPT 大纲草案**：
  1. **封面**：IATF 16949 智能平台 - 重新定义质量标准的学习与应用
  2. **痛点分析**：传统标准学习的枯燥、难以记忆、难以与实际工厂应用结合。
  3. **解决方案**：数字化双向学习流（浏览 -> 测验 -> AI 解析）。
  4. **核心功能演示**：
     - 结构化速查（不再翻厚重的 PDF）
     - 场景化抽认卡与测验
     - 智能条款解读（AI 赋能）
  5. **演进路线与版本历史**
  6. **结语与赞助/联系方式**
- **操作**：编写专用的 Python 脚本直接生成 `.pptx` 文件供您下载和进一步微调。

---

## 下一步行动

如果您认可上述计划，**请告诉我您对【User Review Required】中三个问题的看法**（尤其是关于 API Key 的处理方式）。得到您的回复后，我将立即从“阶段一”开始修改代码。
